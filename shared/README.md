# Shared

Cross-service resources shared between `frontend`, `backend`, and `ml-service`.

## Contents

| File | Purpose |
|---|---|
| `constants.js` | Domain enums and thresholds (risk levels, priority, actions) |

## Adding More

As the project grows, add here:
- Generated OpenAPI TypeScript client (`openapi-generated/`)
- JSON schemas for model payloads (`schemas/`)
- Shared validation helpers
